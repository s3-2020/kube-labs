# KØR: kubectl apply -f .   |   SLUK: kubectl delete -f .
apiVersion: v1
kind: Service
metadata:
  name: whoami-lb
  namespace: lab-red
  annotations: { lbipam.cilium.io/ip-pool: lan-pool }
spec:
  type: LoadBalancer
  selector: { app: whoami }
  ports: [{ port: 80, targetPort: 80 }]

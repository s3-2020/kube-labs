# Chaos-kataer (modul 5)
Regler: apply ÉN ad gangen, diagnose UDEN at læse YAML'en ("hvad ser clusteret?" → status/events/endpoints/logs), reparer, skriv 1 sætning om hvad symptom ≠ årsag var. `kubectl delete -f filen` rydder op.
